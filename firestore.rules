
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users full read and write access to all documents.
    // This is a broad rule for development purposes to resolve persistent permission issues.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
